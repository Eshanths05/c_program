#include <stdio.h>
#include <math.h>

int main()
{
    int n,a,y;
    scanf("%d",&n);
    a=sqrt(n);
    if(n==a*a)
    {
    printf("pn");
    }
    else
    {
        printf("npn");
    }

    return 0;
}